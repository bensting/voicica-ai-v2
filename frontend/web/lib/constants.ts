/** Stable external links that don't need to be env vars — unlike API base
 * URLs or secrets, these don't differ between local/staging/prod and
 * changing one is a one-line code edit, not a config concern. */
export const PLAY_STORE_URL = "https://play.google.com/store/apps/details?id=ai.voicica.app";
